services:
  scielo-proceedings:
    platform: linux/amd64
    build:
      context: .
      dockerfile: docker/rocky9/Dockerfile
    container_name: scielo-proceedings
    ports:
      - "8081:80"
    volumes:
      - ./:/var/www/html
    restart: unless-stopped
